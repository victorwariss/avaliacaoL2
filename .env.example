# Arquivo .env.example
# Copie este arquivo para .env e preencha os valores conforme seu ambiente local.

# Configuração do banco de dados Postgres
DB_HOST=localhost          # Endereço do servidor do banco, ex: localhost ou IP do servidor
DB_PORT=5432               # Porta do banco (5432 padrão do Postgres)
DB_USERNAME=usuario        # Usuário do banco
DB_PASSWORD=senha          # Senha do usuário do banco
DB_DATABASE=nome_do_banco  # Nome do banco de dados a ser usado

# Porta da aplicação (pode deixar 3000 padrão)
PORT=3000
